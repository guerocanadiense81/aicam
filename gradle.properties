# More generous timeouts for flaky networks on CI
org.gradle.jvmargs=-Xmx2g -Dfile.encoding=UTF-8 -Dorg.gradle.internal.http.socketTimeout=120000 -Dorg.gradle.internal.http.connectionTimeout=120000

# Disable Gradle caching on CI so cache outages don’t break builds
org.gradle.caching=false

# AndroidX on by default
android.useAndroidX=true
